# Add ~/.psvm/current/bin to the path
# this is where https://github.com/ThomasCrevoisier/psvm-js
# puts the currently active version of pursescript if you
#      npm install -g psvm
#      psvm install $(psvm latest)
#      psvm use $(psvm latest)
PATH=~/.psvm/current/bin:$PATH
